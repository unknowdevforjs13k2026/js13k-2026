import {debug} from '../state.js';export function initDebug(){document.getElementById('debugButton').addEventListener('click',()=>document.getElementById('debugPanel').classList.toggle('show'));const bind=(id,key)=>{const button=document.getElementById(id);button.addEventListener('click',()=>{debug[key]=!debug[key];button.textContent=debug[key]?'ON':'OFF';button.className=debug[key]?'on':'off'})};[['energyDebug','energy'],['shotEnergyDebug','shotEnergy'],['blackCloudDebug','blackCloud'],['targetDebug','target'],['hintDebug','hint'],['cloudLevelDebug','cloudLevel'],['uiDebug','ui'],['collisionDebug','collision'],['trajectoryDebug','trajectory'],['trajectoryDisplayDebug','trajectoryDisplay'],['shakeDebug','shake']].forEach(x=>bind(...x))}
