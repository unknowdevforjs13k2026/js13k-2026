import {canvas,ctx,W,H,MAX_CHARGE,ARROW_SPEED,clamp} from './core.js';import {aim,weapon,game,debug,clouds,unicorns,backgroundClouds,arrows,particles,impacts,rainbows,rain,weather} from './state.js';import {getCurrentTarget,canCollideCloud} from './game/collision.js';import {getAimPoint} from './game/weapon.js';
export function drawSky(){const g=ctx.createLinearGradient(0,0,0,H);g.addColorStop(0,'#07192f');g.addColorStop(.55,'#4c91ab');g.addColorStop(1,'#d5f3f3');ctx.fillStyle=g;ctx.fillRect(0,0,W,H)}
export function drawBackgroundClouds(){for(const c of backgroundClouds){ctx.save();ctx.globalAlpha=c.opacity;ctx.fillStyle='white';const s=c.size;ctx.beginPath();ctx.ellipse(c.x,c.y+15,s*1.5,s*.4,0,0,Math.PI*2);ctx.fill();ctx.beginPath();ctx.arc(c.x-s*.5,c.y,s*.45,0,Math.PI*2);ctx.arc(c.x,c.y-s*.2,s*.58,0,Math.PI*2);ctx.arc(c.x+s*.5,c.y,s*.45,0,Math.PI*2);ctx.fill();ctx.restore()}}
export function drawRainbow(r){ctx.save();ctx.globalAlpha=clamp(r.life/r.maxLife,0,1);const colors=['#ff4d6d','#ff9f43','#ffe66d','#5de36b','#4ddcff','#6675ff','#d56cff'];for(let i=0;i<colors.length;i++){ctx.strokeStyle=colors[i];ctx.lineWidth=10;ctx.beginPath();ctx.arc(r.x,r.y+r.size*.42,r.size-i*11,Math.PI,Math.PI*2);ctx.stroke()}ctx.restore()}
export function drawCloud(c){ctx.save();ctx.globalAlpha=clamp(c.opacity,.08,1);const s=c.radius;ctx.fillStyle=debug.blackCloud&&c.black?'#20252d':'#effcff';ctx.beginPath();ctx.ellipse(c.x,c.y+15,s*1.2,s*.35,0,0,Math.PI*2);ctx.fill();ctx.beginPath();ctx.arc(c.x-s*.5,c.y,s*.5,0,Math.PI*2);ctx.arc(c.x,c.y-s*.12,s*.62,0,Math.PI*2);ctx.arc(c.x+s*.5,c.y,s*.47,0,Math.PI*2);ctx.fill();if(c.flash>0){ctx.globalAlpha=c.flash;ctx.fillStyle='white';ctx.beginPath();ctx.arc(c.x,c.y,s*.9,0,Math.PI*2);ctx.fill()}if(debug.blackCloud&&c.black&&c.lightningFlash>0){ctx.globalAlpha=c.lightningFlash;ctx.strokeStyle='#dffcff';ctx.lineWidth=2;ctx.shadowBlur=18;ctx.shadowColor='#8beaff';ctx.beginPath();ctx.moveTo(c.x-12,c.y-c.radius*.5);ctx.lineTo(c.x+4,c.y-c.radius*.1);ctx.lineTo(c.x-5,c.y+c.radius*.15);ctx.lineTo(c.x+13,c.y+c.radius*.55);ctx.stroke()}const target=getCurrentTarget();if(debug.target&&target&&canCollideCloud(c)){ctx.globalAlpha=.65+Math.sin(performance.now()/130)*.2;ctx.strokeStyle='#6ceaff';ctx.lineWidth=3;ctx.beginPath();ctx.ellipse(c.x,c.y,s*1.25,s*.72,0,0,Math.PI*2);ctx.stroke()}if(debug.collision){ctx.globalAlpha=.9;ctx.strokeStyle='#ff3333';ctx.lineWidth=1;ctx.beginPath();ctx.arc(c.x,c.y,c.radius,0,Math.PI*2);ctx.stroke()}if(debug.cloudLevel){ctx.globalAlpha=1;ctx.textAlign='center';ctx.font='bold 12px monospace';ctx.fillStyle='#fff45c';ctx.fillText('LV.'+c.level+' HP.'+c.hp+(c.black?' BLACK':''),c.x,c.y-c.radius-12);ctx.textAlign='left'}if(debug.ui){ctx.globalAlpha=1;ctx.fillStyle='#7ff7ff';ctx.font='11px monospace';ctx.fillText('UI:'+c.ui,c.x-25,c.y+c.radius+15)}ctx.restore()}
export function drawUnicorn(u){if(u.rescued)return;ctx.save();ctx.translate(u.x,u.y);ctx.scale(u.scale,u.scale);ctx.globalAlpha=u.opacity;ctx.shadowBlur=16;ctx.shadowColor='#f3aaff';ctx.fillStyle='#f3ddff';ctx.beginPath();ctx.ellipse(0,5,30,17,0,0,Math.PI*2);ctx.fill();ctx.beginPath();ctx.arc(29,-8,14,0,Math.PI*2);ctx.fill();ctx.fillStyle='#fff0a0';ctx.beginPath();ctx.moveTo(31,-18);ctx.lineTo(39,-38);ctx.lineTo(26,-20);ctx.closePath();ctx.fill();ctx.strokeStyle='#f3ddff';ctx.lineWidth=6;for(const x of [-18,-4,10,22]){ctx.beginPath();ctx.moveTo(x,15);ctx.lineTo(x-3,31);ctx.stroke()}ctx.strokeStyle='#c99aff';ctx.lineWidth=7;ctx.beginPath();ctx.moveTo(-28,5);ctx.quadraticCurveTo(-54,0,-43,-20);ctx.stroke();ctx.shadowBlur=0;ctx.fillStyle='#3d2150';ctx.beginPath();ctx.arc(34,-9,2.5,0,Math.PI*2);ctx.fill();ctx.restore();const target=getCurrentTarget();if(debug.target&&target===u){ctx.save();ctx.globalAlpha=.7+Math.sin(performance.now()/100)*.25;ctx.strokeStyle='white';ctx.lineWidth=3;ctx.shadowBlur=18;ctx.shadowColor='white';ctx.beginPath();ctx.arc(u.x,u.y,u.radius+9,0,Math.PI*2);ctx.stroke();ctx.restore()}if(debug.hint&&game.assistFlash>0&&target===u){ctx.save();ctx.globalAlpha=game.assistFlash/2500;ctx.strokeStyle='#ffe85c';ctx.lineWidth=4;ctx.shadowBlur=25;ctx.shadowColor='#ffe85c';ctx.beginPath();ctx.arc(u.x,u.y,u.radius+14,0,Math.PI*2);ctx.stroke();ctx.restore()}if(debug.collision&&target===u){ctx.save();ctx.strokeStyle='#ff3333';ctx.lineWidth=1;ctx.beginPath();ctx.arc(u.x,u.y,u.radius,0,Math.PI*2);ctx.stroke();ctx.restore()}if(debug.ui){ctx.fillStyle='#ffff55';ctx.font='11px monospace';ctx.fillText('UI:'+u.ui,u.x+18,u.y-25)}}
export function drawArrows(){for(const a of arrows){ctx.save();for(let i=1;i<a.trail.length;i++){const p1=a.trail[i-1],p2=a.trail[i];ctx.globalAlpha=(i/a.trail.length)*.55;ctx.strokeStyle='#62eaff';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(p1.x,p1.y);ctx.lineTo(p2.x,p2.y);ctx.stroke()}const len=38,v=Math.hypot(a.vx,a.vy)||1,bx=a.x-a.vx/v*len,by=a.y-a.vy/v*len;ctx.globalAlpha=1;ctx.strokeStyle='white';ctx.lineWidth=4;ctx.shadowBlur=20;ctx.shadowColor='#55eaff';ctx.beginPath();ctx.moveTo(bx,by);ctx.lineTo(a.x,a.y);ctx.stroke();ctx.restore()}}
export function drawRain(){if(!weather.raining)return;ctx.save();ctx.strokeStyle='#d5f8ff';ctx.lineWidth=1;for(const r of rain){ctx.globalAlpha=r.opacity;ctx.beginPath();ctx.moveTo(r.x,r.y);ctx.lineTo(r.x-3,r.y+r.length);ctx.stroke()}ctx.restore()}
export function drawBow(){const bx=W*.09,by=H*.82,p=getAimPoint(),angle=Math.atan2(p.y-by,p.x-bx);ctx.save();ctx.translate(bx,by);ctx.rotate(angle);ctx.strokeStyle='#61eaff';ctx.lineWidth=7;ctx.shadowBlur=18;ctx.shadowColor='#61eaff';ctx.beginPath();ctx.moveTo(-7,-52);ctx.quadraticCurveTo(18,0,-7,52);ctx.stroke();const pull=weapon.charging?weapon.charge/MAX_CHARGE*40:0;ctx.shadowBlur=0;ctx.strokeStyle='#efffff';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(-7,-52);ctx.lineTo(-pull,0);ctx.lineTo(-7,52);ctx.stroke();ctx.fillStyle='white';ctx.shadowBlur=22;ctx.shadowColor='#63eaff';ctx.beginPath();ctx.arc(-pull,0,6,0,Math.PI*2);ctx.fill();ctx.restore()}
export function drawCrosshair(){const p=getAimPoint();ctx.save();ctx.translate(p.x,p.y);const r=12+weapon.shake*.25;ctx.strokeStyle=weapon.charging?'#ffe95c':'#ffffffcc';ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();ctx.beginPath();ctx.moveTo(-r-8,0);ctx.lineTo(-r+2,0);ctx.moveTo(r-2,0);ctx.lineTo(r+8,0);ctx.moveTo(0,-r-8);ctx.lineTo(0,-r+2);ctx.moveTo(0,r-2);ctx.lineTo(0,r+8);ctx.stroke();ctx.restore()}
export function drawTrajectoryDebug(){if(!debug.trajectoryDisplay)return;const startX=W*.09,startY=H*.82,p=getAimPoint(),angle=Math.atan2(p.y-startY,p.x-startX),chargeRatio=clamp(weapon.charge/MAX_CHARGE,0,1),gravity=debug.trajectory?520-chargeRatio*360:0;let x=startX,y=startY,vx=Math.cos(angle)*ARROW_SPEED,vy=Math.sin(angle)*ARROW_SPEED;ctx.save();ctx.strokeStyle='#fff45caa';ctx.lineWidth=1.5;ctx.setLineDash([5,5]);ctx.beginPath();ctx.moveTo(x,y);for(let i=0;i<180;i++){const step=.016;vy+=gravity*step;x+=vx*step;y+=vy*step;ctx.lineTo(x,y);if(x<0||x>W||y<0||y>H)break}ctx.stroke();ctx.setLineDash([]);ctx.restore()}
export function drawParticles(){for(const p of particles){ctx.globalAlpha=clamp(p.life/500,0,1);ctx.fillStyle=p.electric?'white':'#bdf8ff';ctx.beginPath();ctx.arc(p.x,p.y,p.size,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1}export function drawImpacts(){for(const p of impacts){ctx.save();ctx.globalAlpha=p.life/420;ctx.strokeStyle='white';ctx.lineWidth=4;ctx.shadowBlur=25;ctx.shadowColor='#6ceaff';ctx.beginPath();ctx.arc(p.x,p.y,p.radius,0,Math.PI*2);ctx.stroke();ctx.restore()}}
